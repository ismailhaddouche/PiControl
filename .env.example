## Example environment variables for PiControl (do NOT commit real secrets)
# Copia este archivo a .env o exporta variables en systemd EnvironmentFile (/etc/default/picontrol)

# Entorno
FLASK_ENV=production

# Ruta a la BD (en el Pi la app usa /var/lib/picontrol por defecto)
DB_PATH=/var/lib/picontrol/pi_control.db

# Clave secreta para sesiones/firmas
SECRET_KEY=changeme
