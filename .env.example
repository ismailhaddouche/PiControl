## Example environment variables for PiControl (do NOT commit real secrets)
# Copy this file to .env or export variables in systemd EnvironmentFile (/etc/default/picontrol)

# Environment
FLASK_ENV=production

# Database path (on Pi the app uses /var/lib/picontrol by default)
DB_PATH=/var/lib/picontrol/pi_control.db

# Secret key for sessions/signatures
SECRET_KEY=changeme
