## Example environment variables for PiControl (do NOT commit real secrets)
# Copy this file to .env or export variables in systemd EnvironmentFile (/etc/default/picontrol)

# CRITICAL: Secret key for session encryption (REQUIRED in production)
# Generate with: python3 -c "import secrets; print(secrets.token_urlsafe(32))"
# Never use default values in production!
SECRET_KEY=CHANGE-ME-TO-RANDOM-SECRET-KEY-MINIMUM-32-CHARS

# Database directory (default: /var/lib/picontrol)
# The DB file will be created at: {PICONTROL_DB_DIR}/pi_control.db
PICONTROL_DB_DIR=/var/lib/picontrol

# Admin action log path (default: /var/log/picontrol/admin_actions.log)
# PICONTROL_ADMIN_LOG=/var/log/picontrol/admin_actions.log

# RFID pending assignment file (default: /var/lib/picontrol/rfid_assign_pending.json)
# PICONTROL_RFID_PENDING_FILE=/var/lib/picontrol/rfid_assign_pending.json

# RFID reader device path (optional - for evdev HID-keyboard-style readers)
# PICONTROL_RFID_DEVICE=/dev/input/event0

# Enable RC522 SPI reader support (set to 1 to enable)
# PICONTROL_ENABLE_RC522=0

